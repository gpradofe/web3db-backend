# Common configurations for all nodes
NODE_ID=$(hostname)
NETWORK_NAME=decentralized-db-network

# Master node specific configurations
MASTER_IP=node-master
MASTER_PORT=5000

# Worker node specific configurations
WORKER_CORES=2
WORKER_MEMORY=4g

# Add any other configurations as needed